{
  "name": "jinja-quick",
  "version": "1.0.0",
  "description": "This is a basic Jinja2 boilerplate for use with an app engine instance. This is intended to run inside of a docker container. In that case, the only dependency you need to setup is [Docker Toolbox](https://www.docker.com/docker-toolbox)",
  "main": "gulpfile.js",
  "dependencies": {
    "gulp": "^3.9.0",
    "gulp-sass": "^2.0.4",
    "gulp-concat": "^2.6.0",
    "merge-stream": "^1.0.0",
    "path": "^0.12.7",
    "fa": "^1.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
